type UserProfile
  @model
  @searchable
  @auth(rules: [{allow: owner}])
{
  id: ID!
  name: String!
  email: String!
  imageUrl: String!
  location: String!
  income: Int!
  skill: Sting!
  smoking: Boolean!
  drink: Boolean!
  nomadStatus: String!
}
